package services

import (
	"fmt"
	dependences "sensitive/controllers/dependences"
)

func LoadSensitiveWord(mongo *dependences.MongoDBClient, tree *dependences.DFATree) {
	fmt.Println("loadSensitiveWord do do do")
}
